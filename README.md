# SpeakerSync-Mac
Allows user to interface with multiple speakers using Mac's built in Audio MIDI Application Multi-Output setup and BlackHole 2ch. Allows user to change speaker settings, delay for sync, stereo sound, and more straight from the CLI.
